{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "ts-node -r dotenv/config index.ts dotenv_config_path=../../.env.development",
    "build": "tsc",
    "serve": "node -r dotenv/config ../dist/server/index.js dotenv_config_path=../../.env.production",
    "typecheck": "tsc --noEmit",
    "tsc-watch": "tsc --watch"
  },
  "dependencies": {
    "@types/node": "^18.7.14",
    "node-sass": "^6.0.1"
  },
  "devDependencies": {
    "ts-node": "^10.2.1"
  }
}
